# SUVIDHA System Architecture Diagrams

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE LAYER                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │         React Frontend (Vite) - Port 3000                    │  │
│  │  • Multi-language (EN/HI/TA)  • Offline support              │  │
│  │  • Responsive UI              • Accessibility features       │  │
│  └────────────────────────┬─────────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────────┘
                            │ HTTP/REST
                            ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         API GATEWAY LAYER                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Nginx API Gateway - Port 8000                   │  │
│  │  • Request routing  • Load balancing  • Rate limiting        │  │
│  └────────────────────────┬─────────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────────┘
                            │ Routes to appropriate service
                            ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      MICROSERVICES LAYER                             │
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │   Auth   │  │  Electr. │  │   Gas    │  │  Water   │           │
│  │  :3001   │  │  :3002   │  │  :3003   │  │  :3004   │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │Municipal │  │ Payment  │  │  Notify  │  │  Admin   │           │
│  │  :3005   │  │  :3006   │  │  :3008   │  │  :3007   │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                         │
│  │ Security │  │Integration│ │Monitoring│                         │
│  │  :3009   │  │  :3010   │  │  :3011   │                         │
│  └──────────┘  └──────────┘  └──────────┘                         │
└────────────────────────┬────────────────────────────────────────────┘
                         │ Database queries
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         DATA LAYER                                   │
│  ┌────────────────────────┐      ┌────────────────────────┐         │
│  │   PostgreSQL :5432     │      │    Redis :6379         │         │
│  │  • User data           │      │  • Sessions            │         │
│  │  • Bills & payments    │      │  • Cache               │         │
│  │  • Audit logs          │      │  • Queue               │         │
│  └────────────────────────┘      └────────────────────────┘         │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow - User Login Example

```
┌──────────┐
│  User    │  1. Enters username/password in login form
└────┬─────┘
     │
     ↓
┌──────────────────────┐
│  React Client        │  2. POST /api/auth/login
│  (client/src)        │     { username, password }
└────┬─────────────────┘
     │
     │ HTTP Request
     │
     ↓
┌──────────────────────┐
│  Nginx Gateway       │  3. Receives on port 8000
│  (localhost:8000)    │     Routes to auth-service
└────┬─────────────────┘
     │
     │ Proxy Pass
     │
     ↓
┌──────────────────────┐
│  Auth Service        │  4. Validates credentials
│  (server/auth-       │     a. Query PostgreSQL for user
│   service:3001)      │     b. Compare password hash
└────┬─────────────────┘     c. Generate JWT token
     │                       d. Store session in Redis
     │
     ↓
┌──────────────────────┐
│  PostgreSQL          │  5. SELECT * FROM users
│  (localhost:5432)    │     WHERE username = ?
└────┬─────────────────┘
     │
     │ User data returned
     │
     ↓
┌──────────────────────┐
│  Auth Service        │  6. Create JWT token
│                      │     { userId, role, exp }
└────┬─────────────────┘
     │
     │ Store session
     │
     ↓
┌──────────────────────┐
│  Redis               │  7. SET session:token -> userData
│  (localhost:6379)    │     EXPIRE session:token 86400
└────┬─────────────────┘
     │
     │ Session stored
     │
     ↓
┌──────────────────────┐
│  Auth Service        │  8. Return response
│                      │     { success: true, token, user }
└────┬─────────────────┘
     │
     │ Response
     │
     ↓
┌──────────────────────┐
│  React Client        │  9. Store token in localStorage
│                      │     Redirect to dashboard
└──────────────────────┘
```

## 🔐 Service-to-Service Communication

```
┌─────────────────────────────────────────────────────────────┐
│  Example: Electricity Bill Payment Flow                     │
└─────────────────────────────────────────────────────────────┘

Client → Electricity Service → Payment Service → Notification Service

Step 1: Get Bill
┌──────────┐     GET /api/electricity/bills/:id     ┌──────────────┐
│  Client  │ ──────────────────────────────────────→│ Electricity  │
└──────────┘                                        │   Service    │
                                                    └──────┬───────┘
                                                           │
                                                           ↓
                                                    ┌──────────────┐
                                                    │ PostgreSQL   │
                                                    │ electricity  │
                                                    │   _bills     │
                                                    └──────────────┘

Step 2: Initiate Payment
┌──────────┐     POST /api/electricity/pay          ┌──────────────┐
│  Client  │ ──────────────────────────────────────→│ Electricity  │
└──────────┘     { billId, amount }                 │   Service    │
                                                    └──────┬───────┘
                                                           │
                                 HTTP POST                 │
                                 /api/payment/initiate     │
                                                           ↓
                                                    ┌──────────────┐
                                                    │   Payment    │
                                                    │   Service    │
                                                    └──────┬───────┘
                                                           │
                                                           ↓
                                                    ┌──────────────┐
                                                    │  Razorpay/   │
                                                    │  Paytm API   │
                                                    └──────────────┘

Step 3: Send Notification
                                                    ┌──────────────┐
                                                    │   Payment    │
                                                    │   Service    │
                                                    └──────┬───────┘
                                                           │
                                 HTTP POST                 │
                                 /api/notifications/send   │
                                                           ↓
                                                    ┌──────────────┐
                                                    │ Notification │
                                                    │   Service    │
                                                    └──────┬───────┘
                                                           │
                                                           ↓
                                                    ┌──────────────┐
                                                    │  Twilio/     │
                                                    │  MSG91 API   │
                                                    └──────────────┘
```

## 📁 Code Organization & Dependencies

```
┌─────────────────────────────────────────────────────────────┐
│  File Structure & Import Dependencies                       │
└─────────────────────────────────────────────────────────────┘

client/
├── src/
│   ├── features/
│   │   └── electricity/
│   │       ├── components/
│   │       │   └── BillPayment.jsx  ──┐
│   │       ├── services/              │
│   │       │   └── api.js  ───────────┼── Imports
│   │       └── hooks/                 │
│   │           └── useBills.js  ──────┘
│   │
│   ├── shared/                        ┌── Shared utilities
│   │   ├── components/  ──────────────┤   used across
│   │   ├── utils/      ──────────────┤   all features
│   │   └── constants/  ──────────────┘
│   │
│   └── config/
│       └── api.js  ─── Imports from shared/constants/endpoints.js

server/
├── auth-service/
│   ├── src/
│   │   ├── controllers/
│   │   │   └── auth.controller.js  ──┐
│   │   ├── services/                 │
│   │   │   └── auth.service.js  ─────┼── Imports
│   │   ├── routes/                   │
│   │   │   └── auth.routes.js  ──────┘
│   │   └── models/
│   │       └── user.model.js  ─── Imports from shared/types/
│
├── electricity-service/
│   └── src/
│       ├── controllers/
│       ├── services/  ─── May call auth-service via HTTP
│       └── routes/
│
└── shared/  ──────────────────────── Used by all services
    ├── types/       (TypeScript definitions)
    ├── constants/   (Endpoints, status codes)
    ├── utils/       (Validators, formatters)
    └── middlewares/ (Auth verification)
```

## 🌐 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                      PRODUCTION DEPLOYMENT                           │
└─────────────────────────────────────────────────────────────────────┘

OPTION 1: Split Deployment (Free Tier)
┌──────────────────────┐
│  Vercel (Frontend)   │  client/ → Build → CDN distribution
│  Free: 100GB/month   │  URL: https://suvidha.vercel.app
└──────────┬───────────┘
           │ API calls to →
           ↓
┌──────────────────────┐
│  Railway (Backend)   │  server/auth-service → Docker container
│  Free: 500hrs/month  │  URL: https://suvidha-api.up.railway.app
│  + PostgreSQL        │
│  + Redis             │
└──────────────────────┘

OPTION 2: Oracle Cloud (Best - Always Free)
┌─────────────────────────────────────────────────────────────────────┐
│                Oracle Cloud VM (1GB RAM, Always Free)                │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Docker Containers                           │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────────┐        │  │
│  │  │Client│ │Auth  │ │Elec. │ │ ... │ │ PostgreSQL   │        │  │
│  │  │(80)  │ │(3001)│ │(3002)│ │     │ │    (5432)    │        │  │
│  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────────────┘        │  │
│  └───────────────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────────────────────────┘
             │
             ↓
┌────────────────────────┐
│  Cloudflare (CDN)      │  Free SSL, DDoS protection, Global CDN
│  Free tier             │  URL: https://suvidha.yourdomain.com
└────────────────────────┘
```

## 🔄 Local Development Workflow

```
Day 1: Setup
┌────────────────────────────────────────────────────────────┐
│  Developer Machine                                         │
│                                                            │
│  1. git clone https://github.com/GUNA777448/SUVIDHA-kiosk│
│  2. cd SUVIDHA-kiosk                                      │
│  3. ./scripts/setup.ps1  ← Installs dependencies         │
│  4. docker-compose up -d postgres redis  ← Start DBs     │
│  5. npm run db:migrate  ← Create tables                  │
│  6. npm run dev  ← Start all services                    │
│                                                            │
│  ✅ Ready to develop!                                     │
└────────────────────────────────────────────────────────────┘

Day-to-Day Development
┌────────────────────────────────────────────────────────────┐
│  Terminal 1                   Terminal 2                   │
│  ┌──────────────────┐        ┌──────────────────┐         │
│  │ cd client        │        │ cd server/auth   │         │
│  │ npm run dev      │        │ npm run dev      │         │
│  │                  │        │                  │         │
│  │ ✓ Vite HMR       │        │ ✓ Nodemon reload│         │
│  │ ✓ Auto-refresh   │        │ ✓ Auto-restart  │         │
│  └──────────────────┘        └──────────────────┘         │
│                                                            │
│  Make changes → Auto-reload → Test → Commit → Push       │
└────────────────────────────────────────────────────────────┘

Testing
┌────────────────────────────────────────────────────────────┐
│  npm test              ← Run all tests                     │
│  npm run test:watch    ← Watch mode                        │
│  npm run test:coverage ← Coverage report                   │
└────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│  User Journey: Pay Electricity Bill                                 │
└─────────────────────────────────────────────────────────────────────┘

1. User Login
   User → Client → Auth Service → PostgreSQL → Redis
         ↓
      JWT Token stored

2. View Bills
   Client → Electricity Service → PostgreSQL
         ↓
      Bills displayed

3. Initiate Payment
   Client → Electricity Service → Payment Service → Razorpay
                                        ↓
                                  Transaction ID

4. Verify Payment
   Razorpay webhook → Payment Service → PostgreSQL (update)
                              ↓
                        Notification Service → SMS/Email

5. Audit Log
   All services → Security Service → PostgreSQL (audit_logs)

6. Receipt Generation
   Payment Service → PDF generation → Storage → Client download
```

---

## 🎯 Summary

**Interconnections:**

- **Client** calls **Nginx** which routes to appropriate **Service**
- **Services** query **PostgreSQL** for data, **Redis** for cache
- **Services** can call other services via HTTP
- **All services** share common code from `shared/` folder

**Local Run:**

- `npm run dev` starts everything
- Hot reload on code changes
- All services run on different ports
- Single database, shared by all

**Zero-Cost Deploy:**

- **Frontend**: Vercel/Netlify/Cloudflare Pages
- **Backend**: Oracle Cloud (best) or Railway
- **Database**: Oracle Autonomous DB or Railway PostgreSQL
- **Domain**: Freenom (free) or Cloudflare
- **SSL**: Let's Encrypt (free)
